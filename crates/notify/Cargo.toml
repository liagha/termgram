[package]
name = "termgram-notify"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
termgram = { path = "../core" }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde_json = "1"